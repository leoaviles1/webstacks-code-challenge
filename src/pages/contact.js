import * as React from "react";
// import { Link } from "gatsby";
import Layout from "../layouts/layout";

const ContactPage = () => (
  <Layout pageTitle='Contact' pageHeading='Contact Us'>
    <p>Now this is the time for all good people</p>
  </Layout>
);

export default ContactPage;
